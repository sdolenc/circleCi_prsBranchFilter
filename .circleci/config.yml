# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details
#
version: 2
jobs:
  sleep1:
    working_directory: ~/repo
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run:
          name: long test
          command: sleep 123

#todo: transition to working solution and comment out this block
workflows:
  version: 2
  all:
    jobs:
      - sleep1:
          filters:
            branches:
              only:
                - master
